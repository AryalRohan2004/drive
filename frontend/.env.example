# Backend API base URL
# Local development (Vite proxy handles forwarding to http://localhost:5000)
VITE_API_BASE=/api

# Production example (set this in your hosting provider's env vars)
# VITE_API_BASE=https://your-backend-url.com/api
